<div class="page-border">
	<%= form_for @category, url: borrower_category_path(@category) do |f| %>
    <span class="form-title">
      <%= f.label :name %>
    </span><br/>
		<%= f.text_field :name %><br/><br/>
		<%= f.submit class: 'btn btn-primary'%>
	<% end %>
</div>
